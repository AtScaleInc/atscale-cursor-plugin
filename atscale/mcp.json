{
  "mcpServers": {
    "atscale": {
      "url": "${ATSCALE_MCP_URL}",
      "auth": {
        "CLIENT_ID": "atscale-mcp",
        "CLIENT_SECRET": "${ATSCALE_MCP_CLIENT_SECRET}"
      }
    }
  }
}
